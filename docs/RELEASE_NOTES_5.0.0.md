# Multi-Script 5.0.0 — Customization Studio

Players can personalize identity, themes, accent, scale, density, width, typography, corner style, backdrop and motion. ElevenLabs now has an in-settings, per-user local API-key flow. Keys are password-masked, sent only to the localhost bridge, stored in runtime/.env with private permissions, never persisted in browser storage, and excluded from releases.
