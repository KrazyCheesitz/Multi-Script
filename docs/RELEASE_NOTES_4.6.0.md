# Multi-Script 4.6.0 — Provider QA release

This public-release candidate adds exhaustive tool-call compatibility tests across all nine supported AI websites and safer Arena human-verification handling. Every direct tool is round-tripped through every provider contract. Arena Direct mode was also smoke-tested on the live public page. CAPTCHA challenges are never bypassed: Multi-Script moves out of the way, pauses, and resumes only after the user completes verification manually.
